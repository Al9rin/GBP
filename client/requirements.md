## Packages
framer-motion | Complex animations for step transitions and progress bars
clsx | Conditional class merging
tailwind-merge | Intelligent tailwind class merging
lucide-react | Icons for UI elements

## Notes
Using Replit Auth (use-auth.ts) for user management.
Backend provides /api/progress for tracking completion.
Layout switches significantly between mobile (bottom nav) and desktop (sidebar).
Custom color palette derived from GoodTherapy brand (Orange #F58220, Blue #0056b3).
