# Required: PostgreSQL connection string
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Required: Secret for session encryption (use a long random string)
SESSION_SECRET=your-session-secret-here

# Optional: Port to serve the app on (default: 5000)
PORT=5000

# Required for Replit Auth: Set to your Replit instance ID
# Use "local-dev-placeholder" for local development (skips auth)
REPL_ID=local-dev-placeholder
